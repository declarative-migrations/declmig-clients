    [package]
    name = "declmig-client"
    version = "0.1.0"
    edition = "2021"
    license = "MIT"
    description = "Typed HTTP client for Declarative Migrations"
    repository = "https://github.com/declarative-migrations/declmig-clients"

    [dependencies]
    serde = { version = "1", features = ["derive"] }
    serde_json = "1"
    thiserror = "2"

[lints.rust]
unsafe_code = "forbid"


