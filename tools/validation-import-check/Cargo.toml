[package]
name = "declmig-validation-import-check"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
description = "Fail-closed validator for public validation SDK consumer boundaries"
license = "MIT"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"

[dev-dependencies]
tempfile = "3"

[workspace]
